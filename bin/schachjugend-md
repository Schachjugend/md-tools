#!/usr/bin/env node

var program = require('commander');

program
  .version(require('../package.json').version)
  .command('spielordnung [format]', 'Spielordnung konvertieren')
  .parse(process.argv);
